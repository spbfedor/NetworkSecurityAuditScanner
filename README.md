# Network Security Audit Scanner 🛡️🔍

Асинхронный инструмент для сетевого аудита и сбора баннеров сервисов. Разработан с фокусом на производительность, архитектурную чистоту и безопасность.

## 🌟 Ключевые особенности
- **Async Engine**: Высокая скорость сканирования благодаря использованию `asyncio` (кооперативная многозадачность).
- **Smart Banner Grabbing**: Автоматическое определение типа сервиса. Поддержка обычного текста (SSH, FTP) и SSL/TLS рукопожатий (HTTPS) для извлечения информации о версиях ПО.
- **Robust Architecture**: Логика инкапсулирована в класс `NetworkAuditScanner`, настройки вынесены в отдельный JSON-конфиг.
- **DNS Intelligence**: Асинхронное разрешение имен (DNS lookup) перед началом работы.
- **Enterprise Logging**: Полноценное логирование событий в консоль и файл `scanner.log`.
- **Docker-Ready**: Полная контейнеризация проекта для быстрого развертывания в любой среде.

## 🛠 Технологический стек
- **Language**: Python 3.12+
- **Core**: `asyncio`, `socket`, `ssl`
- **DevOps**: Docker, Docker Compose
- **Data**: JSON, Logging

## 🚀 Быстрый старт

### 1. Настройка
Отредактируйте файл `config.json`, указав целевой хост и список интересующих портов:
```json
{
    "target": "scanme.nmap.org",
    "ports": [22, 80, 443],
    "timeout": 1.5
}
```

## 2. Запуск через Docker (рекомендуется)
Проект полностью готов к работе в контейнере. Одной командой вы соберете образ и получите результат:

```bash
docker-compose up --build
```

## 3. Локальный запуск
Если у вас установлен Python 3.12+:
```bash
python scanner.py
```

## 📊 Пример отчета (scan_report.json)
```json
[
    {
        "port": 22,
        "status": "open",
        "banner": "SSH-2.0-OpenSSH_6.6.1p1 Ubuntu-2ubuntu2.13"
    },
    {
        "port": 443,
        "status": "open",
        "banner": "HTTP/1.1 200 OK"
    }
]
```